import "./Header.css"
const Header = ()=>{
    return (
        <nav>
            <div id="navList">
            <a href="#" className="home">HOME</a>
            <a href="#">ABOUT</a>
            <a href="#">CONTACT</a>
            </div>
            
        </nav>
    )
}

export default Header;